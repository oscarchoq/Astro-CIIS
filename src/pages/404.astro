---
export const prerender = true;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>404 - Página no encontrada</title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      html {
        background-color: #000415;
        min-height: 100vh;
        overflow: hidden;
      }

      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        color: #fff;
        position: relative;
      }

      /* Estrellas animadas de fondo */
      .stars {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none;
      }

      .star {
        position: absolute;
        background: white;
        border-radius: 50%;
        animation: twinkle 3s infinite;
      }

      @keyframes twinkle {
        0%, 100% { opacity: 0.3; }
        50% { opacity: 1; }
      }

      /* Contenedor principal */
      .container {
        text-align: center;
        z-index: 10;
        padding: 2rem;
        max-width: 600px;
      }

      /* Número 404 */
      .error-code {
        font-size: 10rem;
        font-weight: 900;
        background: linear-gradient(45deg, #6e91d6, #3a7bd5, #00d2ff);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 1rem;
        animation: glow 2s ease-in-out infinite alternate;
        line-height: 1;
      }

      @keyframes glow {
        from {
          filter: drop-shadow(0 0 10px rgba(110, 145, 214, 0.5));
        }
        to {
          filter: drop-shadow(0 0 30px rgba(110, 145, 214, 0.8));
        }
      }

      /* Astronauta flotante */
      .astronaut {
        font-size: 5rem;
        animation: float 3s ease-in-out infinite;
        display: inline-block;
        margin-bottom: 2rem;
      }

      @keyframes float {
        0%, 100% { transform: translateY(0px) rotate(-5deg); }
        50% { transform: translateY(-20px) rotate(5deg); }
      }

      h1 {
        font-size: 2.5rem;
        margin-bottom: 1rem;
        color: #e5eaf2;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
      }

      p {
        font-size: 1.2rem;
        color: #b0b8d4;
        margin-bottom: 2rem;
        line-height: 1.6;
      }

      /* Botón de regreso */
      .btn {
        display: inline-block;
        padding: 0.75rem 2rem;
        background: transparent;
        color: #6e91d6;
        text-decoration: none;
        border: 2px solid #6e91d6;
        border-radius: 8px;
        font-weight: 500;
        font-size: 1rem;
        transition: all 0.3s ease;
        position: relative;
      }

      .btn:hover {
        background: rgba(110, 145, 214, 0.1);
        border-color: #3a7bd5;
        color: #3a7bd5;
        transform: translateY(-2px);
      }

      .btn:active {
        transform: translateY(0);
      }

      /* Planetas decorativos */
      .planet {
        position: fixed;
        border-radius: 50%;
        opacity: 0.3;
        animation: orbit 20s linear infinite;
      }

      .planet-1 {
        width: 80px;
        height: 80px;
        background: linear-gradient(135deg, #ff6b6b, #ee5a6f);
        top: 10%;
        right: 10%;
      }

      .planet-2 {
        width: 60px;
        height: 60px;
        background: linear-gradient(135deg, #4ecdc4, #44a08d);
        bottom: 15%;
        left: 15%;
        animation-duration: 15s;
        animation-direction: reverse;
      }

      @keyframes orbit {
        0% { transform: rotate(0deg) translateX(50px) rotate(0deg); }
        100% { transform: rotate(360deg) translateX(50px) rotate(-360deg); }
      }

      /* Responsive */
      @media (max-width: 768px) {
        .error-code {
          font-size: 6rem;
        }

        h1 {
          font-size: 1.8rem;
        }

        p {
          font-size: 1rem;
        }

        .astronaut {
          font-size: 3rem;
        }

        .btn {
          padding: 0.8rem 2rem;
          font-size: 1rem;
        }
      }
    </style>
  </head>
  <body>
    <!-- Estrellas animadas -->
    <div class="stars" id="stars"></div>

    <!-- Planetas decorativos -->
    <div class="planet planet-1"></div>
    <div class="planet planet-2"></div>

    <!-- Contenido principal -->
    <div class="container">
      <div class="astronaut">🚀</div>
      <div class="error-code">404</div>
      <p>Parece que te has perdido en el espacio. Esta página no existe en nuestro universo.</p>
      <a href="/" class="btn">Volver al inicio</a>
    </div>

    <script>
      // Generar estrellas aleatorias
      const starsContainer = document.getElementById('stars');
      if (starsContainer) {
        for (let i = 0; i < 100; i++) {
          const star = document.createElement('div');
          star.className = 'star';
          star.style.width = Math.random() * 3 + 'px';
          star.style.height = star.style.width;
          star.style.left = Math.random() * 100 + '%';
          star.style.top = Math.random() * 100 + '%';
          star.style.animationDelay = Math.random() * 3 + 's';
          starsContainer.appendChild(star);
        }
      }
    </script>
  </body>
</html>


